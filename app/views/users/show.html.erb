<div class="window">   
    <% provide(:title, @user.username) %>
    <div class="profilepic">
        <%= link_to image_tag(@user.image.url(:large)) %>
        <h1 class="namebelow">welcome back, <%= @user.username %></h1>
    </div>
</div>